console.log("Bom dia, Olá denovo"); //sentença, um conjunto de objeto e método(normalmente) com o seu valor
//Influenciam no programa ou no site, são sentenças, são comandos mesmo que fazem parte de algo maior.

{
    let  total,a,b;
    a=15;
    b=  ++a + a;
    total=(a+b)*4; //aqui está se fazendo a multiplicação primeiro a*4 e b*4 e depois soma
    console.log( total); //sentença
}

console.log(a);  // não escreve 15, por que a var a está restrita a um bloco só 

// A ordem dos comandos importa quando colocada dentro do código!

